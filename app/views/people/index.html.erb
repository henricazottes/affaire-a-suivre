<h1>Personnes</h1>

<ul>
  <% @people.each do |person| %>
    <li>
      <%= person.name %>
      <%= link_to 'Show', person %>
    </li>
  <% end %>
</ul>

<%= link_to "Nouvelle personne", new_person_path %>
